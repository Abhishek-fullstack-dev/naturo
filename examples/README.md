# Examples

Example scripts and automation recipes will be added as features are implemented.

## Planned Examples

- `notepad_hello.py` — Open Notepad, type text, save (Phase 2)
- `window_capture.py` — Capture screenshots of all windows (Phase 1)
- `ui_inspector.py` — Interactive UI tree browser (Phase 1)
- `form_filler.py` — Auto-fill a Windows form (Phase 2)
- `agent_demo.py` — AI agent driving a Windows app (Phase 4)
